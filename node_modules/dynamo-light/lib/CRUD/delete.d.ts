import { DeleteCommandInput, DeleteCommandOutput } from "@aws-sdk/lib-dynamodb";
import { IDLArgumentsBase } from "../types";
interface IDLDelete extends IDLArgumentsBase<DeleteCommandInput> {
    forTrx: boolean;
    key: any;
}
/**
 * Deletes an item from the table
 */
export default function deleteItem({ docClient, tableName, key, options, verbose, forTrx, }: IDLDelete): Promise<DeleteCommandOutput | {
    Delete: any;
}>;
export {};
