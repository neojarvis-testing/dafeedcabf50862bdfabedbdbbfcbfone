import { PutCommandInput } from "@aws-sdk/lib-dynamodb";
import { IDLArgumentsBase } from "../types";
interface IDLPut extends IDLArgumentsBase<PutCommandInput> {
    autoTimeStamp?: boolean;
    forTrx?: boolean;
    item: any;
}
/**
 * Create Item in table, 'createdAt' and 'updatedAt' timeStamps will be added in each item
 */
export default function create({ docClient, tableName, item: rawItem, options, verbose, forTrx, autoTimeStamp, }: IDLPut): Promise<PutCommandInput | {
    Put: PutCommandInput;
}>;
export {};
