import { ScanCommandInput } from "@aws-sdk/lib-dynamodb";
import { IDLArgumentsBase } from "../types";
interface IDLScan extends IDLArgumentsBase<ScanCommandInput> {
    pagination: boolean;
    indexName: string;
}
/**
 * Get all items from table
 */
export default function getAll({ docClient, tableName, indexName, options, pagination, verbose, }: IDLScan): Promise<any>;
export {};
