import { GetCommandInput, GetCommandOutput } from "@aws-sdk/lib-dynamodb";
import { ResponseMetadata } from "@aws-sdk/types/dist-types/response";
import { IDLArgumentsBase } from "../types";
interface IDLGet extends IDLArgumentsBase<GetCommandInput> {
    key: any;
    forTrx: boolean;
}
export interface IDLGetOutput extends Omit<GetCommandOutput, "$metadata"> {
    Get?: GetCommandInput;
    $metadata?: ResponseMetadata;
}
/**
 * Get Item from table
 */
export default function get({ docClient, tableName, key, options, verbose, forTrx, }: IDLGet): Promise<IDLGetOutput>;
export {};
