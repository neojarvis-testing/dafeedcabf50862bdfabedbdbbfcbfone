import { QueryCommandInput } from "@aws-sdk/lib-dynamodb";
import { IDLArgumentsBase } from "../types";
interface IDLQuery extends IDLArgumentsBase<QueryCommandInput> {
    indexName: string | undefined;
    pagination: boolean;
    partitionKey: any;
    partitionKeyValue: any;
    sortKey: any;
    sortKeyOperator: string | undefined;
    sortKeyValue: any;
}
export default function query({ docClient, tableName, indexName, partitionKey, partitionKeyValue, sortKey, sortKeyOperator, sortKeyValue, options, pagination, verbose, }: IDLQuery): Promise<any>;
export {};
