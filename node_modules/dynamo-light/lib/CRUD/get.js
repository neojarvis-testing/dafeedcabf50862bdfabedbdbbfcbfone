"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const lib_dynamodb_1 = require("@aws-sdk/lib-dynamodb");
/**
 * Get Item from table
 */
function get({ docClient, tableName, key, options = {}, verbose = false, forTrx = false, }) {
    return __awaiter(this, void 0, void 0, function* () {
        /**
         * Param verification
         */
        if (!key) {
            throw new Error("key is undefined!");
        }
        /**
         * Construct database request
         */
        const params = Object.assign({ TableName: tableName, Key: key }, options);
        if (verbose) {
            console.log("params", params);
        }
        try {
            /**
             * Return params for this requirement, used for transact method
             */
            if (forTrx) {
                return {
                    Get: params,
                };
            }
            const data = yield docClient.send(new lib_dynamodb_1.GetCommand(params));
            if (verbose) {
                console.log(`Successfully got item from table ${tableName}`, data);
            }
            if (!data.Item) {
                data.Item = undefined;
                // throw new Error(`No item found with key = ${JSON.stringify(key)}`);
            }
            return data;
        }
        catch (err) {
            if (verbose) {
                console.error(`Unable to get item from ${tableName}. Error JSON:`, JSON.stringify(err), err.stack);
                console.log("Error request params: ", JSON.stringify(params));
            }
            throw err;
        }
    });
}
exports.default = get;
